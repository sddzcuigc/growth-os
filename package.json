{
  "name": "talent-task-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node api/server.js",
    "check": "node --check app.js && node --check api/server.js && node scripts/validate-product-logic.mjs",
    "test:e2e": "playwright test tests/e2e/workflow.spec.js --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
